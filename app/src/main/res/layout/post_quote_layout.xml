<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:ignore="MissingPrefix"
    android:orientation="vertical">

    <TextView
        android:id="@+id/quote_text_view"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        fontPath="charter_regular.ttf"
        android:textSize="@dimen/item_font_size_medium"
        android:contentDescription="@string/quote_text_description"
        android:layout_marginTop="5dp"/>

    <TextView
        android:id="@+id/quote_source_view"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="5dp"
        android:textSize="@dimen/item_font_size_small"
        android:contentDescription="@string/quote_source_description"
        tools:text="" />

</LinearLayout>